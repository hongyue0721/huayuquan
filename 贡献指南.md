# 词库呈批贡献指南（官方修订版）

**发文字号**：贡词〔2026〕1号  
**密级**：公开  
**时效**：自发布之日起施行

---

## 一、什么是词库呈批

本设备之词库由**内置词库**（`terms/seed.json`、`terms/common.json`）与**贡献词库**（`terms/contrib.json`）构成。内置词库为站长钦定，轻易不动；贡献词库面向全体同志开放——凡有佳译，皆可呈报。

「词库呈批」即：贡献者依规定格式在呈批专窗呈报词条之增删，呈批机器人即时校验格式，格式合规者准予收录、自动转呈拉取请求并代为御批，词条即刻入库生效；格式不合者驳回，请依模板重新呈递。

## 二、如何呈批

请于[词库呈批模板](https://github.com/hongyue0721/huayuquan/issues/new?template=%E8%AF%8D%E5%BA%93%E5%91%88%E6%89%B9.md)新建呈文（亦可直接新建标题为「【词库呈批】」的 issue，于正文依格式填写），呈报后无需任何操作，机器人自会料理后续。

## 三、呈报格式规范

每行一条，句式二选一：

```
添加: 原词 = 译名
删除: 原词
```

**示例**：

```
添加: bug = 虫子
添加: base url = 请求地址
删除: solar
```

**格式须知**：

1. 冒号支持半角（`:`）与全角（`：`），等号支持半角（`=`）与全角（`＝`），可任意混用；
2. 空行、以 `#` 开头的行（注释）将被忽略，可借以分隔条目、撰写批注；
3. 原词须为小写英文原词（多词以空格分隔，如 `base url`、`windows 11`）；
4. 译名须为规范现代汉语——官方定名优先（如 token→词元、agent→智能体），次取直译腔（如 Python→蟒蛇、bug→虫子），不得残留英文字母或阿拉伯数字；
5. 整份呈文任何一行不合格式，即整份驳回，请依驳回通知逐条改正后重新呈递。

## 四、流转流程

```
贡献者呈报 → 机器人校验 → 准予 / 驳回
                              │
                     准予 → 自动呈报分支（petition/issue-N）
                              → 收录词条至 terms/contrib.json、重建 词库.js
                              → 自动开拉取请求
                              → 机器人代为御批合并 → 词库生效
```

- **校验**：正文解析成方案（`呈批方案.json`），格式有误或所呈为空（全空、全注释）即驳回，驳回通知列明每一处不合格式之行号与原因；
- **准予**：机器人在分支上收录所呈词条、重建网页词典，开出拉取请求并当场代为御批合并（合并不成则留待站长亲批），将准予批复（附拉取请求链接）回复于呈文之下；
- **生效**：拉取请求合并后，新增词条随词典重建即刻生效。

## 五、注意事项

1. **删除仅针对贡献词库**：`删除` 只能移除 `terms/contrib.json` 中已收录的词条，动不了内置词库（`terms/seed.json`、`terms/common.json`）——内置词条如需删改，请直接向站长反映；
2. **一词目前一译**：同一原词请勿呈报多个译名；新增词条若与内置词库同名，以合并后的内置优先级为准（`seed.json` 高于 `contrib.json` 高于 `common.json`，`cache.json` 最高）；
3. **恶意灌水必被驳回**：格式不合、乱填乱译、大量无意义条目者，一律驳回；屡教不改者，站长可依职权不予理睬；
4. **呈文可编辑重批**：已呈报之呈文（issue）可在获批前编辑修改，机器人按最新正文重新校验，复用原拉取请求。

---

**本指南由项目组根据实际运行情况定期修订，解释权归项目发起方所有。**  
**修订日期**：二零二六年八月十三日
